<Window
    x:Class="Text_Grab.Views.SpeakParagraphOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Speak Paragraph"
    AllowsTransparency="True"
    Background="Transparent"
    KeyDown="Window_KeyDown"
    Loaded="Window_Loaded"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    Topmost="True"
    WindowState="Maximized"
    WindowStyle="None"
    mc:Ignorable="d">
    <Grid>
        <Image
            x:Name="BackgroundImage"
            Opacity="0.85"
            Stretch="UniformToFill" />
        <Rectangle Fill="#44000000" />
        <Canvas
            x:Name="ParagraphCanvas"
            Cursor="Hand"
            MouseDown="ParagraphCanvas_MouseDown"
            MouseMove="ParagraphCanvas_MouseMove" />
        <TextBlock
            x:Name="HintText"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            Margin="0,0,0,24"
            FontSize="14"
            Foreground="#CCFFFFFF"
            Text="Click a paragraph to speak it  |  Esc to cancel" />
    </Grid>
</Window>
